本轮下注  = Σ totalBuySol           (活跃用户)
本轮成本  = 本轮下注 - currentHoldersRealized
已落袋    = Σ (totalSellSol - totalBuySol)   (已退出用户)
浮盈浮亏  = Σ (netTokenReceived × currentPrice - netSolSpent)  (活跃用户)

本轮成本
本轮下注  
 已落袋    
浮盈浮亏  

在用户列表上面 加上 实时交易 列表 。 按最新的交易排序 样式为下面切图 /www/wwwroot/gmgn-extension-react/dataweb/屏幕截图2.png   ， 当前用户列表 ，增加 字段  本轮成本 ，本轮下注  已落袋  浮盈浮亏  显示每个用户 的数据


净流入部分数据字体调大 比例大一些
需要：（加上usdt和sol的切换）
散户净流入（散户净流入有正有负的相加）
散户本轮净流入（已清仓的算作0，正数的净流入相加）
散户已实现利润（散户的总卖出减去总买入）
散户未实现利润（没清仓的散户的未实现利润总和）
庄手续费（庄家账号的手续费gas总和）
庄利润（用数据2减去数据3减去数据5得出的就是庄的为实现利润）

散户榜单
去掉占比和数量，加上usdt和sol的切换
勾选-地址（备注）-分数-净流入-本轮净流入-持仓价值-已实现利润-未实现利润-gas费-类

暂时先这么多，看看哪些能获取

指标: 是否持仓，  历史持仓，

下注：本轮净流入，清仓过的重置只算本次实际买入的净流入    散户本轮净流入    | 持仓用户 ，清仓重置 归零，
落袋：散户已实现利润           散户已实现利润（散户的总卖出减去总买入）    | ？未持仓用户   清仓过的 历史持仓   
浮盈：散户为实现利润     散户未实现利润（没清仓的散户的未实现利润总和）
成本：下注减去落袋



四个指标的计算公式
1. 💰 已落袋  (yiLuDai)
适用对象：已退出的用户（代币余额 < 1）
公式：
已落袋 = Σ (卖出总额 - 买入总额)
// 对所有已退出用户求和
yiLuDai += (stats.totalSellSol - stats.totalBuySol)
含义：所有已经清仓用户的实现盈亏总和

2. 🎯 本轮下注 (benLunXiaZhu)
适用对象：当前持有者（代币余额 ≥ 1）
公式：
本轮下注 = Σ (买入总额)
// 对所有当前持有者求和
benLunXiaZhu += stats.totalBuySol
含义：当前所有持有者的买入总投入

3. 💵 本轮成本 (benLunChengBen)
适用对象：当前持有者
公式：
本轮成本 = 本轮下注 - 当前持有者的卖出总额
benLunChengBen = benLunXiaZhu - currentHoldersRealized
详细说明：
本轮下注：当前持有者的买入总额
currentHoldersRealized：当前持有者已经卖出回收的 SOL
结果：当前持有者的净成本（还锁定在代币中的 SOL）
4. 📈 浮盈浮亏 (floatingPnL)
适用对象：当前持有者
公式：
浮盈浮亏 = Σ (当前持有代币市值 - 净成本)
// 对所有当前持有者求和
对于每个持有者：
  当前市值 = 持有代币数量 × 当前价格
  净成本 = netSolSpent (买入花费 - 卖出收入)
  浮盈浮亏 += (当前市值 - 净成本)
代码实现：




指标: 是否持仓，  历史持仓 ，最近未清仓净流入

历史持仓：就是清仓过的历史交易
当前持仓:最近一轮未清仓的买卖交易
本轮下注：需是当前 持仓用户 ，历史持仓又清仓的不算，只算当前持仓净流入，就是未清仓的交易
落袋：散户已实现利润     用户   清仓过的 历史持仓  就流水 
浮盈浮亏：散户为实现利润     散户未实现利润（没清仓的散户的未实现利润总和）
成本：下注减去落袋

【
排序顺序：这个功能对 Sig 的排序 有极高要求。如果排序反了，先算“买 300”再算“卖 100（清之前的仓）”，逻辑会完全错乱。请确保使用我之前建议的 Slot + Index 排序。
转账干扰：如果用户把币 Transfer 走了，netTokenReceived 也会变 0。你需要确保 MetricsEngine 也能处理转账类型的 sig，否则他转走了币，你却认为他还在持仓，会导致“本轮下注”虚高。

1. 建立物理排序权重（The Sorting Key）
当你从 Helius、RPC 节点或 GMGN 拿到数据后，第一步是统一字段并进行多级排序。Solana 的交易顺序由以下三个属性唯一确定：

一级权重：Slot (区块高度) —— 决定了交易发生在哪个时间片段。

二级权重：Transaction Index (块内索引) —— 决定了在同一个区块中，哪笔交易先被执行。

三级权重：BlockTime (时间戳) —— 作为辅助参考，但前两者已经足够确定物理顺序。

2. 验证“逻辑链条”：前因后果校验
由于你是多方获取，最怕的是中间漏掉了一笔（比如漏掉了一笔卖出，导致余额算错）。
每一笔正确的交易序列必须满足：交易 A 的“后余额” = 交易 B 的“前余额”。

数据字段标准化：确保每一条 sig 数据都带有 slot、transactionIndex、preTokenBalance、postTokenBalance。

前置排序器：在输入 MetricsEngine 之前，先跑一遍 sort。

断裂监控：在 updateTraderState 中加入 pre/post 余额不等的报警逻辑

】


我拿到 最近的 一个 sig 通过 卖出的 和剩余的 持有量 是否可以判断出来

本轮下注 我确定的定义是 : 1.  历史清仓的用户交易 不在计算， 计算 最近的 持仓的 净流水 ，比如 A用户 之前 买卖 很多 最后一笔清仓 ，就不在计算了。 B 用户 ，买一笔100 卖一笔100 全卖完了  有买1笔300 卖笔各 50 60 一直都没有卖完 对后买三笔 就要 计算 净流水  ，实现这个功能 需要 准备什么 调整什么 


历史持仓：就是清仓过的买卖交易
当前持仓:最近一轮未清仓的买卖交易

本轮下注：用户的 当前持仓 净流水 ， 历史持仓不算
已落袋：历史持仓净流水 合计
浮盈浮亏：历史持仓净流水+当前持仓净流水
本轮成本:本轮下注-已落袋



轮次的定义
每个用户有两部分：


completedRounds[]   ← 历史已完结轮次（数组）
currentRound        ← 当前正在进行的轮次
一轮 = 从某次买入开始，到持仓归零（Token余额 < 1）结束的完整买卖周期。

举例说明
历史0轮 — 该用户从未清仓过，只有当前轮次：


用户A：买入 0.5 SOL → 持仓中
completedRounds = []        ← 历史 0 轮
currentRound = { buy=0.5, sell=0 }
历史1轮 — 该用户完整走过一个买卖周期后，又开始了新一轮：


用户B：
  第一次买入 1 SOL → 卖出 1.2 SOL → Token归零 → 轮次关闭
  第二次买入 0.3 SOL → 还在持仓

completedRounds = [{ buy=1.0, sell=1.2, netFlow=+0.2 }]  ← 历史 1 轮
currentRound = { buy=0.3, sell=0 }
轮次关闭触发条件
在 MetricsEngine.js:271：


if (action === '卖出' && stats.netTokenReceived < 1) {
    // Token余额 < 1 视为清仓，关闭当前轮次
    const closedRound = {
        buySOL, sellSOL,
        netFlow: sellSOL - buySOL,  // 正=盈利，负=亏损
        ...
    };
    stats.completedRounds.push(closedRound);
    stats.currentRound = { buySOL:0, sellSOL:0 };  // 重置
}
对应4大参数的计算
参数	来源
已落袋	Σ completedRounds[i].netFlow（所有历史轮次净流水之和）
本轮下注	currentRound.buySOL - currentRound.sellSOL（当前轮净投入）
浮盈浮亏	已落袋 + 当前轮净流水
本轮成本	本轮下注 - 已落袋
所以：

历史0轮 → 该用户从未完整卖出过，已落袋贡献 = 0
历史N轮 → 该用户完整走过 N 次买卖，每轮的盈亏都计入已落袋